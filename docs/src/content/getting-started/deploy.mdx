---
title: 'Deploying Your Astro Project'
date: '2025-01-08'
description: 'A comprehensive guide to deploying Astro projects on Vercel and Netlify.'
draft: false
type: 'Getting Started'
---

import {SquarePen, Rocket, Code } from 'lucide-react'
import Card from '../../components/Card.astro'
import CardGrid from '../../components/docs/CardGrid.astro'
import ArticleSection from '../../components/docs/ArticleSection.astro'
import CodeBlock from '../../components/docs/CodeBlock.astro'

<br>
### Deploying to Vercel
</br>
Below are the steps to deploy your Astro project to Vercel.

<ArticleSection>
#### Step 1: Install Vercel CLI

First, install the Vercel CLI globally if you haven't already:

<CodeBlock lang="bash" code={`npm install -g vercel`} />

#### Step 2: Login to Vercel

Authenticate your CLI with your Vercel account:

<CodeBlock lang="bash" code={`vercel login`} />

#### Step 3: Deploy the Project

Navigate to your project's root directory and run the deploy command:

<CodeBlock lang="bash" code={`vercel
`} />
</ArticleSection>

### Deploying to Netlify

Here's how you can deploy your Astro project to Netlify.

<ArticleSection>
#### Step 1: Install Netlify CLI

Begin by installing the Netlify CLI globally:

<CodeBlock lang="bash" code={`npm install -g netlify-cli`} />

#### Step 2: Login to Netlify

Authenticate your CLI with your Netlify account:

<CodeBlock lang="bash" code={`netlify login`} />

#### Step 3: Update `astro.config.mjs` for Netlify

Replace the Vercel adapter with Netlify's adapter in your `astro.config.mjs`:

<CodeBlock lang="javascript" code={`import netlify from '@astrojs/netlify';
import mdx from '@astrojs/mdx';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind';
import { defineConfig } from 'astro/config';
import astroDocs from './integrations/astroDocs';

export default defineConfig({
  output: 'hybrid',
  adapter: netlify({
    functions: {
      // Optional: Specify the directory for Netlify Functions
      directory: 'netlify/functions',
    },anchors: [
        {
        //rest of the file
        ]}
`} />

#### Step 4: Build and Deploy

Run the build command to generate the `dist` directory:

<CodeBlock lang="bash" code={`npm run build`} />

Deploy the project to Netlify:

<CodeBlock lang="bash" code={`netlify deploy --prod --dir=dist
`} />

Follow the prompts to complete the deployment. Netlify will automatically handle the rest.

</ArticleSection>

## Revisit some docs

<CardGrid>
  <Card
    header='Build Doc'
    content="Steps to install, build, update this doc."
    href='/getting-started/quickstart'
    withIcon>
    <Code className='stroke-primary' />
  </Card>
  <Card
    header='Create Pages'
    content='Add and organize new documentation pages with ease.'
    href='/getting-started/creating-pages'
    withIcon>
    <SquarePen className='stroke-primary' />
  </Card>
  <Card
    header='Use Components'
    content='Leverage pre-built custom components for rich documentation.'
    href='/getting-started/components'
    withIcon>
    <Rocket className='stroke-primary' />
  </Card>
</CardGrid>
