---
title: MandleBrot Coloring
description: Cpp app that draws the Mandelbrot fractal and allows you to zoom in and explore the fractal (uses zero libraries).
---

import { ControlsTable, GroupName, Album, TextBlock }  from '../../../components/components'
import { ColoringMandlebrotSamples } from '../../../data/albumImages.ts'

<div class="space-y-0">
<GroupName>Mandlebrot</GroupName>
# Mandelbrot Coloring
</div>

<TextBlock size="lg" maxWidth="3xl" weight="medium" isProse invertInDark>
SFML-based Mandelbrot viewer program.
</TextBlock>

<Album title="Colored Samples" images={ColoringMandlebrotSamples} columns={1} />

## Requirements
 * C++17
 * CMake 3.22

## Building & Running

```shell
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release --target run
```

### Controls

<ControlsTable
  controls={[
    { key: "Click", description: "Go to point"},
    { key: "Scroll or W/S", description: "Zoom" },
    { key: "Arrow Keys", description: "Pan" },
    { key: "[ ]", description: "Change iterations" },
    { key: "R", description: "Reset view" },
  ]}
/>

